#include <stdio.h>
#include <stdlib.h>
#include "math.h"

int main()
{
int a=input_data (1);
int b=input_data (2);
int c=input_data (3);
if (c==0)
{
    printf (" на нуль ділити не можна\n");
}else{
printf("C^3 = %f\n", power (c, 3));
printf("sum = %ld\n", sum (0, a) );
printf(" Q = %f\n", q(a, b, c));
return 0;
}
}
