#define _CRT_SECURE_NO_WARNINGS 
#include "Header.h"
#include <stdio.h>
#include <math.h>
#include <stdlib.h>
#include <malloc.h>

int main(int argc, const char* argv[]) {

  while (1){
 double A = 0.0, 
 B = 0.0, 
 C = 0.0;

double numerator = 0.0;
double denominator = 0.0;
double res_Fraction = 0.0;
  int res_Sigma = 0;

   double* pA = &A;
   double* pB = &B;
   double* pC = &C;

  WARNING();
  
  //значение от польхователя
  input2(&A, &B, &C);

  //вычисляем числитель и з и знаменатель
  numc(&numerator, &A, &B, &C);
  demc(&denominator, &B, &C);
//дробная часть
  fraction(&res_Fraction, &numerator, &denominator);

  //sum
  sigma(&A, &Res1);

  //ответ
  f_Answer(&res_Fraction, &Res1);


  }

  return 0;

}
