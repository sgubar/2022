#include "tools.h"

int main()
{
    int A = get_A();
    float B = get_B();
    float C = get_C();
    int fact = factorial(A);
    float upp = up(A, B, C);
    float result = calc(upp, fact, A, C);
    show_value(result);
    return 0;
}
